function CronOutputComponent({children}: {children: React.ReactNode}){
    return (
        <div className=" flex justify-center item-center py-10 border border-white mb-20 rounded text-2xl">
            {children}
        </div>
    )
}

export default CronOutputComponent